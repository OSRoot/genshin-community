import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'os-genshin-community',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
